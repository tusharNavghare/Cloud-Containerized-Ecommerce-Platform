apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce
spec:
  selector:
    app: order-service
  ports:
    - port: "<PORT_NUMBER>"
      targetPort: "<TARGET_PORT_NUMBER>"
