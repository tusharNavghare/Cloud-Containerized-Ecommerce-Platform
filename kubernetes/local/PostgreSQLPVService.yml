apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ecommerce
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: "<PORT_NUMBER>"
      targetPort: "<TARGET_PORT_NUMBER>"